<Window x:Class="QuizWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz - Aplikacja WPF" Height="591" Width="600"
        Background="#F5F5F5" ResizeMode="CanMinimize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblNumerPytania" 
                   Grid.Row="0"
                   Text="Pytanie 1 z 4" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Foreground="#34495e"
                   Margin="0,0,0,20"/>

        <TextBlock x:Name="lblPytanie" 
                   Grid.Row="1"
                   Text="Treść pytania" 
                   FontSize="20" 
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Foreground="#2c3e50"
                   Margin="0,0,0,20"/>

        <StackPanel x:Name="stackOdpowiedzi" 
                    Grid.Row="2"
                    VerticalAlignment="Top"/>

        <TextBlock x:Name="lblStatus" 
                   Grid.Row="3"
                   FontSize="18" 
                   FontWeight="Bold"
                   TextAlignment="Center"
                   Margin="0,20,0,10"/>

        <Button x:Name="btnNastepne" 
                Grid.Row="4"
                Content="Następne pytanie" 
                Height="45"
                FontSize="16"
                Background="#3498db"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                Click="BtnNastepne_Click"
                Visibility="Collapsed"/>
    </Grid>
</Window>